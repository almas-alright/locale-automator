# locale-automator
Localization automator bot


## install

- Install dependencies:
  - Run: `yarn install`
- Install this project globally
  - Run : `npm install -g`

## usage

- initialize
    - localize_auto init
